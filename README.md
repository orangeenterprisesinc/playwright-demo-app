# Repo A Application

This repository contains a minimal Node.js Express application that serves a static homepage and a simple health endpoint.

## Available commands

- `npm install` - install dependencies
- `npm start` - start the app locally
- `npm run build` - run the build placeholder

## Endpoints

- `/` - serves the static homepage
- `/api/health` - returns a JSON health status
#testing